apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "outbox-service.fullname" . }}-test-connection"
  labels:
    {{- include "outbox-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "outbox-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
