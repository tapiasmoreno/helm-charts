apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springboot-service.fullname" . }}-test-connection"
  labels:
    {{- include "springboot-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springboot-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
